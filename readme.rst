Ctrl-Digit
==========

It's not complicated: allows Mac and Linux versions of Firefox to use Ctrl+1-9 to switch tabs like Windows does, so that I don't hammer at the wrong shortcut like a dopamine-addled lab rat when I hop platforms.